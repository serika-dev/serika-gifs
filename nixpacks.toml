# Nixpacks configuration for Coolify deployment

[phases.setup]
nixPkgs = ["openssl", "libuuid"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
